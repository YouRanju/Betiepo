<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
	
<!-- DB 연동하는 페이지 필요 -->

<%
	String user = (String)session.getAttribute("isLogin");
	
	/* 
	DB
	
	SELECT * FROM userportpolio WHERE username = user(변수);
	*/
%>